# Ramji Bakery - Project Fix Plan

## Current Project Status

### Architecture
- **Frontend**: Next.js 14 (Pages Router) with Tailwind CSS ✓ KEEPING
- **Backend**: Express.js server with file-based JSON storage (separate deployment)
- **Deployment**: Vercel for frontend + separate Vercel for backend

### Key Components
1. **Client** (`/client`): Next.js frontend with Pages Router
2. **Server** (`/server`): Express.js API with file-based storage

---

## Issues to Fix

### 1. Speed Insights NOT Integrated
- **Status**: Speed Insights not installed
- **Fix**: Add `@vercel/speed-insights` package to client/package.json

### 2. Missing Client Dependencies
- Need to check if npm deps are properly installed

### 3. Build Verification Needed
- Must verify client builds successfully

---

## Implementation Steps

### Step 1: Install Speed Insights
- Edit `client/package.json` to add `@vercel/speed-insights` dependency
- Run `cd client && npm install`

### Step 2: Integrate Speed Insights in _app.js
- Import and wrap app with SpeedInsights component

### Step 3: Configure Images in next.config.js
- Add external image domains for Cloudinary uploads
- Add proper image optimization config

### Step 4: Build and Verify
- Run client production build
- Fix any build errors

### Step 5: Vercel Deployment
- Deploy client to Vercel
- Deploy server to Vercel (as separate project or API routes)
- Set NEXT_PUBLIC_API_URL environment variable

---

## Files to Update

1. `client/package.json` - Add speed-insights
2. `client/pages/_app.js` - Add SpeedInsights wrapper  
3. `client/next.config.js` - Add image domains

---

## Deployment Steps

### For Frontend (Vercel):
```bash
cd client
npm install
npm run build
```
Then deploy the `.next` folder or connect GitHub repo.

### Environment Variables Needed:
- `NEXT_PUBLIC_API_URL` = backend Vercel deployment URL

### For Backend (Separate Vercel):
Deploy server folder as separate Vercel project with custom server or convert Express routes to Vercel API routes.